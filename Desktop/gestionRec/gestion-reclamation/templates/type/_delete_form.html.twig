<form method="post" action="{{ path('app_type_delete', {'idtype': type.idtype}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type.idtype) }}">
    <button class="btn">Delete</button>
</form>
